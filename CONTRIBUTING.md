# Contributing

Open an issue before proposing a contract-breaking change. Keep the service-claim API small, add tests for validation or rendering behavior, and update the relevant ADR when changing a control boundary.

Before opening a pull request, run:

```bash
make verify
```

Do not include cloud credentials, kubeconfigs, production hostnames, or employer-owned source material.
