# Security policy

Do not open a public issue for a suspected vulnerability. Use GitHub's **Report a vulnerability** flow under the repository Security tab and include affected versions, reproduction steps, impact, and any proposed mitigation.

Only the latest revision of `main` is supported. Never include cloud credentials, kubeconfigs, production hostnames, tokens, or employer-owned configuration in a report or pull request.
